# meshopt showcase video

Remotion source for the 15-second horizontal showcase video introducing
`Milky2018/meshopt_mbt`.

## Commands

```bash
npm install
npm run studio
npm run still
npm run render
```

Generated previews and videos are written to `out/`, which is intentionally
ignored by git.
